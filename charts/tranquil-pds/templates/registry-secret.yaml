---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-registry-key
data:
  .dockerconfigjson: {{ required "Docker registry config docker.dockerconfigjson is required" .Values.docker.dockerconfigjson | quote }}
type: kubernetes.io/dockerconfigjson

